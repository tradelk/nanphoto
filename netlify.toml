# Конфигурация для деплоя на Netlify
# https://docs.netlify.com/configure-builds/file-based-configuration/
# Next.js определяется автоматически (OpenNext), publish directory задаётся сам.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"
